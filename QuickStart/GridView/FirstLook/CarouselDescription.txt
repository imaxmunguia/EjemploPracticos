The Business Grid demo allows to preview the selected grid item; edit the selected record by using the Details panel on the right; insert a new record by pressing the [NEW] button in the command item; update/insert rows using outside edit form; delete the selected item by pressing the [DELETE] button from the command item.